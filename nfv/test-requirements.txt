# The order of packages is significant, because pip processes them in the order
# of appearance. Changing the order has an impact on the overall integration
# process, which may cause wedges in the gate later.
#
# Minimum versions verified against StarlingX debian trixie/upper-constraints.txt
# under Python 3.13.
hacking>=8.0.0 # Apache-2.0
bandit>=1.8.0
coverage>=7.0.0
fixtures>=4.1.0 # Apache-2.0/BSD
pylint>=3.0.0
stestr>=4.1.0
testtools>=2.7.2 # MIT
pyjwkest>=1.4.4
oic>=1.7.0

# Runtime dependencies required to import/run nfv packages under py3.13.
# Listed here so trixie tox envs do not need to repeat them inline.
iso8601>=2.1.0
keyring>=25.6.0
kombu>=5.5.3
kubernetes>=35.0.0
oslo.log>=7.1.0
passlib>=1.7.4
pecan>=1.5.1
pyparsing>=3.1.2
PyYAML>=6.0.2
sqlalchemy>=2.0.0
stevedore>=5.4.1
wsme>=0.12.0
