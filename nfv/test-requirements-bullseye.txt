# The order of packages is significant, because pip processes them in the order
# of appearance. Changing the order has an impact on the overall integration
# process, which may cause wedges in the gate later.
#
# Minimum versions verified against StarlingX debian upper-constraints.txt
# under Python 3.9.
hacking>=7.0.0 # Apache-2.0
bandit>=1.8.0
coverage>=7.0.0
fixtures>=4.2.6 # Apache-2.0/BSD
pylint>=2.17.0
stestr>=4.1.0
testtools>=2.7.2 # MIT
pyjwkest>=1.4.4
oic>=1.7.0

# Runtime dependencies required to import/run nfv packages.
# Listed here so tox envs do not need to repeat them inline.
iso8601>=0.1.13
keyring>=22.0.1
kombu>=5.0.2
kubernetes>=35.0.0
oslo.log>=4.4.0
passlib>=1.7.4
pecan>=1.3.3
pyparsing>=2.4.7
PyYAML>=6.0
sqlalchemy>=1.4.47
stevedore>=5.0.0
wsme>=0.11.0
